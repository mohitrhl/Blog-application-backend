services:
  # PostgreSQL database
  db:
    image: postgres:latest
    restart: always

    ports:
      - "5432:5432"

    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}

  # Database management interface
  adminer:
    image: adminer:latest
    restart: always

    ports:
      - "8888:8080"